console.log(Math.round(1.6)); //round: 반올림 함수
console.log(Math.round((1.4));

function sum(first, second){  // first, second: 매개변수(parameter)

  return first+second;
  console.log('A') // 이 줄은 sum 함수를 돌려도 실행되지 않는다. return은 그 함수의 종결지점이기도 함
};

console.log(sum(2,4)); // 2,4: 인자(argument) 
