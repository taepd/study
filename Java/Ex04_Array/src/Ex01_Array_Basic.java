//배열은(Array) 객체다
//1. 배열은 객체이므로 (new를 통해서 생성)
//2. new를 통해서 heap 메모리에 생성된다
//3. 고 정배열(정적 배열) : 배열의 크기는 한 번 정해지면 고정된다




public class Ex01_Array_Basic {

    public static void main(String[] args) {
        int s, s1, s2, s3;
        int[] score = new int[3]; //방을 3개 만듬   //방 번호(첨자, index)
        score[0]=10;
        score[1]=20;
        score[2]=30;
        
    }

}
